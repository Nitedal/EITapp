<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eitapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".language_list" />
        <activity android:name=".infoScreen" />
        <activity android:name=".LoggonScreen" />
        <activity android:name=".RegisterNewUser" />
        <activity android:name=".Mainscreen" />
        <activity android:name=".RegistrerNewActivity" />
        <activity android:name=".activitySelectionMain" />
        <activity android:name=".ActivitySelectPhysicalActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".FindActivity" />
        <activity android:name=".Calender"></activity>
    </application>

</manifest>